const sharedNotes = (state) => state.notesShare.sharedNotes;
const isShared = (state) => state.notesShare.isShared;
const urlPath = (state) => state.notesShare.urlPath;

export { sharedNotes, isShared, urlPath };
